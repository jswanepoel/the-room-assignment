import { RegistrationStatusResponse } from './registration-status.response';

describe('RegistrationStatusResponse', () => {
  it('should be defined', () => {
    expect(new RegistrationStatusResponse()).toBeDefined();
  });
});
