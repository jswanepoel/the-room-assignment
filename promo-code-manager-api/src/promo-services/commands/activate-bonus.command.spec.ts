import { ActivateBonusCommand } from './activate-bonus.command';

describe('ActivateBonusCommand', () => {
  it('should be defined', () => {
    expect(new ActivateBonusCommand()).toBeDefined();
  });
});
