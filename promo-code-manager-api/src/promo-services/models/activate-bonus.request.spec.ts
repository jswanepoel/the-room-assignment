import { ActivateBonusRequest } from './activate-bonus.request';

describe('ActivateBonusRequest', () => {
  it('should be defined', () => {
    expect(new ActivateBonusRequest()).toBeDefined();
  });
});
