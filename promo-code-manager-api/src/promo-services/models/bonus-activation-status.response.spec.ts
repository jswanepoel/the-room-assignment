import { BonusActivationStatusResponse } from './bonus-activation-status.response';

describe('BonusActivationStatusResponse', () => {
  it('should be defined', () => {
    expect(new BonusActivationStatusResponse()).toBeDefined();
  });
});
