import { PromoServiceBuilder } from './promo-service.builder';

describe('PromoServiceBuilder', () => {
  it('should be defined', () => {
    expect(new PromoServiceBuilder()).toBeDefined();
  });
});
