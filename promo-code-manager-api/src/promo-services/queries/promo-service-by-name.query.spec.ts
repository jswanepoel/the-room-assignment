import { PromoServiceByNameQuery } from './promo-service-by-name.query';

describe('PromoServiceByNameQuery', () => {
  it('should be defined', () => {
    expect(new PromoServiceByNameQuery()).toBeDefined();
  });
});
